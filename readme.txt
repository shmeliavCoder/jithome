install:

run the folder build the image of githubapi and run it on port 8080

docker build .

docker run -p 8080:8888 <hash>

then install my package (the hello-cli folder) with:

npm i -g .

call the cli:

hello -n <numberOfPackages>

notice that there is another file that contains my own version of web crawling

